server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://20.45.1.35:8099/eureka/

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: custom-service
          uri: lb://custom-service
          predicates:
            - Path=/custom/**